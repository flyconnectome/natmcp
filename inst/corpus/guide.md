# natverse guide (orientation map)

This file is the hand-authored, Tier-3 backbone of the `guide` tool: the
altitude map an agent needs *before* it can search sensibly. Keep it short and
outcome-focused; per-function detail comes from `signature`/`find`.

## What altitude to reach for

<!-- When to use cf_* (coconatfly, cross-dataset) vs dataset-specific clients
     (neuprintr, fafbseg) vs low-level nat. To be written. -->

## Package roles

<!-- One line each for Tier-A core packages: what layer, what it's for.
     Seeded from DESCRIPTION Title/Description at build time, then curated. -->

## Dataset roster

<!-- Short pointer into the nv_datasets table; see the `dataset` tool. -->

## How the pieces compose

<!-- Typical pipelines: ids -> partners -> cosine; neuron -> mesh (Rvcg) ->
     plot (rgl); neuron -> graph (igraph). To be written. -->
