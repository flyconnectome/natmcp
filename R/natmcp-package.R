#' natmcp: grounded natverse knowledge for coding agents
#'
#' natmcp builds a live index of natverse ground truth and serves it to coding
#' agents over the Model Context Protocol (MCP). See [build_index()] for the
#' ingestion pipeline, [natmcp_mcp_server()] for the server, and
#' [natmcp_tools()] for the tools as ellmer tools. The tools exposed are
#' `guide`, `find`, `signature`, `snippet`, `dataset` and `lint`.
#'
#' @keywords internal
"_PACKAGE"
