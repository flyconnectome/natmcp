% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serve.R
\name{natmcp_tools}
\alias{natmcp_tools}
\title{natmcp tools as a list of ellmer tools}
\usage{
natmcp_tools(tools = NULL, index = NULL)
}
\arguments{
\item{tools}{Optional character vector selecting tool groups (e.g.
\code{"guide"}, \code{"find"}, \code{"signature"}, \code{"snippet"}, \code{"dataset"}, \code{"lint"});
\code{NULL} returns all.}

\item{index}{Path to a built index (see \code{\link[=build_index]{build_index()}}).}
}
\value{
A list of \code{ellmer::tool()} objects.
}
\description{
Returns the natmcp tools as \code{\link[ellmer:tool]{ellmer::tool()}} objects. Because they are plain
ellmer tools they work both over MCP (via \code{\link[=natmcp_mcp_server]{natmcp_mcp_server()}}) and when R
is the client (\code{ellmer::Chat$set_tools()}). The API mirrors
\code{btw::btw_tools()} so the two compose: pass \code{c(btw::btw_tools(), natmcp_tools())} to a single \code{\link[mcptools:mcp_server]{mcptools::mcp_server()}}.
}
