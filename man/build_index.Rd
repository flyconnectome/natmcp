% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.R
\name{build_index}
\alias{build_index}
\title{Build the natmcp index}
\usage{
build_index(
  config = system.file("config", "packages.yml", package = "natmcp"),
  out_dir = file.path("inst", "index"),
  scope = c("offline", "online")
)
}
\arguments{
\item{config}{Path to the scrape config
(default the packaged \code{inst/config/packages.yml}).}

\item{out_dir}{Directory to write the index + manifest into
(default \code{inst/index}).}

\item{scope}{\code{"offline"} (CI-runnable core) or \code{"online"} (enrichment
overlay needing CAVE/neuprint auth).}
}
\value{
Invisibly, the path to the written index directory.
}
\description{
Runs the ingestion pipeline: resolve sources from the scrape config,
introspect installed packages (namespaces, formals, Rd, executed examples),
harvest machine-readable artefacts (pkgdown reference groups, vignettes,
examples), fold in the curated corpus (guide, dataset table, snippets), and
emit the index plus a reproducibility manifest.
}
\details{
Sourcing is org-driven and source-first. The builder enumerates each org in
the config (all repos are candidates), classifies each repo by the presence
of a \code{DESCRIPTION} (package -> indexed; otherwise -> corpus-only), applies
\code{overrides}, and records the git SHA + package version of everything it
touches in \code{manifest.json}. Return shapes are captured by executing package
\verb{@examples} in a sandboxed \link[callr:callr]{callr} process, which doubles as a freshness
check.

This is the CI-side half of the deployment split (Option C): \code{build_index()}
runs in GitHub Actions (which has natverse installed) and publishes a
versioned index artifact; servers never build, they \code{\link[=fetch_index]{fetch_index()}}. The
\code{scope} distinguishes the CI-runnable offline core from the online
enrichment that needs live dataset tokens.

Not yet implemented -- Phase 1 scaffolds structure and configuration only.
Implemented in Phase 2 (Tier-1 introspection) onward per \code{PLAN.md}.
}
